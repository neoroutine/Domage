name: domage
description: Dofus data inspector.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  drift:
  sqlite3_flutter_libs:
  path_provider: 
  path:

  english_words: ^4.0.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  drift_dev:
  build_runner:

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
  - assets/test.db
